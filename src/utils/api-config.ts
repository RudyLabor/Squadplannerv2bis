// 🔧 API Configuration
// Centralized configuration for API client

/**
 * BYPASS MODE Configuration
 * 
 * Set to false to use Edge Functions (recommended)
 * Set to true to use direct KV access (only for debugging JWT issues)
 * 
 * ✅ Edge Functions are working properly - BYPASS MODE SHOULD BE FALSE
 */
export const USE_BYPASS_MODE = true;
